<tr id="<%= dom_id category %>" class="hover:bg-gray-700/50 transition duration-150 ease-in-out group">
<%# Note que usei a classe group na tr; final da linha acima.  
Os botões de Editar/Excluir ficam invisíveis para não poluir a tela, e só aparecem quando você passa o mouse sobre a linha.
%>
  <td class="px-6 py-4 font-mono text-gray-500">
    #<%= category.id %>
  </td>

  <td class="px-6 py-4 font-medium text-white">
    <%= category.name %>
  </td>

  <td class="px-6 py-4 text-right">
    <div class="flex justify-end gap-3 opacity-0 group-hover:opacity-100 transition-opacity duration-200">

      <%= link_to edit_admin_category_path(category), class: "p-2 rounded hover:bg-blue-600/20 text-blue-400 transition" do %>
      <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path>
      </svg>
      <% end %>

      <%= button_to admin_category_path(category), 
          method: :delete, 
          class: "p-2 rounded hover:bg-red-600/20 text-red-400 transition",
          form: { data: { turbo_confirm: "Tem certeza que deseja excluir a categoria '#{category.name}'?" } } do %>
      <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path>
      </svg>
      <% end %>

    </div>
  </td>
</tr>