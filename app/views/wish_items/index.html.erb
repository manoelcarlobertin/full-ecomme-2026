<div class="min-h-screen bg-zinc-50 dark:bg-zinc-950 py-8">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">

    <div class="flex items-center justify-between mb-8">
      <div>
        <h1 class="text-2xl font-bold text-zinc-900 dark:text-white">Minha Lista de Desejos</h1>
        <p class="mt-1 text-sm text-zinc-500 dark:text-zinc-400">
          Você tem <span class="font-medium text-blue-500"><%= @wish_items.count %></span> itens salvos.
        </p>
      </div>

      <%= link_to "Continuar comprando", root_path, class: "text-sm text-blue-600 hover:text-blue-500 dark:text-blue-400 hover:underline" %>
    </div>

    <% if @wish_items.any? %>
      <div id="wish_items_grid" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
        <%= render @wish_items %>
      </div>
    <% else %>
      <div class="text-center py-20 bg-white dark:bg-zinc-900 rounded-2xl border border-dashed border-zinc-300 dark:border-zinc-700">
        <div class="mx-auto h-12 w-12 text-zinc-400">
          <svg class="h-12 w-12" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z" />
          </svg>
        </div>
        <h3 class="mt-2 text-sm font-medium text-zinc-900 dark:text-white">Lista vazia</h3>
        <p class="mt-1 text-sm text-zinc-500 dark:text-zinc-400">Você ainda não favoritou nenhum produto.</p>
        <div class="mt-6">
          <%= link_to "Explorar Produtos", root_path, class: "inline-flex items-center px-4 py-2 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 transition" %>
        </div>
      </div>
    <% end %>

  </div>
</div>